export enum RoutePath {
  HOME = "/",
  LOGIN = "/login",
  REGISTRATION = "/registration",
  WISHLIST = "/wishlist",
  ADD_WISH = "/wishlist/add",
  FRIENDS = "/friends",
  PROFILE = "/profile",
  NOT_FOUND = "*",
}
